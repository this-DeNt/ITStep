#pragma once
#include "framework.h"
#include "classframework.h"

class Laptop {

    private:

        const std::string _name;

        CPU _cpu;
        GPU _gpu;
        RAM _ram;
        SSD _ssd;
        Keyboard _keyboard;
        TouchPad _touchPad;
        FlashDrive * _flashDrive;
        Mouse * _mouse;
        Speakers * _speakers;
    
    public:

        
};